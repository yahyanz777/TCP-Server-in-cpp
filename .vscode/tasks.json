{
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: make build project",
            "command": "make",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": {
                "base": "$gcc"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task to build the project using makefile."
        }
    ],
    "version": "2.0.0"
}